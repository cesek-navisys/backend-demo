/**
 * je totéž, co controller - tzn. bude mít stejné metody i parametry
 * jako parametry bude používat interfaces
 *
 * findOne(aacountCode: string, accountCode: string, query?: IAccountQueryOne)
 *
 */
